#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>

void getUI(char *buf[5]);

int main() {
  char *buffer[5];
  size_t len = 32;
  size_t charac;

  getUI(&buffer);

  return 0;
}

void getUI(char *buf[5]) {
  int count = 0;
  while (1) {
    if (count > 4) {
      count = 0;
    }
    printf("Enter Input: ");
    scanf("%s", &buf[count]);
    count++;
  }
}
