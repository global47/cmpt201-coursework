#include <stdio.h>
#include <unistd.h>
#include <sys/types.h>

int main() {
  int pid = getpid();
  int ppid = getppid();

  // printf("start: %d, child: %d\n", ppid, pid);

  pid_t pids = fork();

  int ppid2 = getppid();
  int pid2 = getpid();

  if (pids == ppid2) {
    execl("/bin/ls", "/bin/ls", "/home", "-a", NULL);
  }

  if (pids == pid2) {
    execl("/bin/ls", "/bin/ls", "/home", "-a", NULL);
  }
}
